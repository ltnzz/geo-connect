import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

export function LoginScreen() {
  return (
    <PlaceholderScreen
      title="Login"
      description="Firebase Auth entry point. Add email, password, and social login flows here."
    />
  );
}
