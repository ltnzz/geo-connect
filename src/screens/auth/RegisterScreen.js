import { PlaceholderScreen } from '../../components/common/PlaceholderScreen';

export function RegisterScreen() {
  return (
    <PlaceholderScreen
      title="Register"
      description="Create a Firebase Auth account and a matching users document here."
    />
  );
}
