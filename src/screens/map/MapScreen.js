import TabPlaceholder from '../../components/common/TabPlaceholder';

export default function MapScreen() {
  return (
    <TabPlaceholder
      icon="map-outline"
      subtitle="Nearby posts, venues, and events will appear on this map."
      title="Maps"
    />
  );
}
